on:
  push:
    branches:
      - main
  #schedule:
  #- cron: '15 4,5 * * *'

jobs:
  health_check:
    runs-on: ubuntu-latest
    steps:
      - name: Check the deployed service URL
        uses: jtalk/url-health-check-action@v4
        with:
          url: full-stack-open-pokedex-fragrant-river-8693.fly.dev/health
